请仅对现有langflow工作流节点src\lfx\src\lfx\components\doubao进行UI样式重构，严格保持整体背景色不变，精准模仿图片的UI设计风格。不修改任何功能逻辑、文字内容（仅允许新增必要按钮文字），重点调整以下部分：
1. 实时预览区域
区域本质：将现有的「实时预览」区域重构为纯预览容器，样式参考图二的黑色方框区域：
保留 8px 圆角 + 1px 浅灰色边框（#E0E0E0）
无任何功能列表文字（如“尝试：”“图生图”等），仅作为结果展示区
当无生成结果时：显示占位文本 暂无生成结果（居中对齐，14px 字体，#999 颜色）
当有生成结果时：直接显示图片/视频，保留原节点的放大/缩放交互逻辑（仅优化视觉样式）
新增上传按钮：
在预览框正上方添加 上传图片 按钮（中文）
按钮样式：12px 字体，浅灰色文字（#666），无边框，与预览框左侧对齐
交互：点击后弹出中文指引的上传页面（页面内容：标题“选择图片” + 按钮“从设备上传” + 提示“支持JPG/PNG格式”）
2.输入与参数区域
提示文字区：在「创作提示词」输入框上方，添加图二风格的说明文字：
“描述你想要生成的内容，并在下方调整生成参数。（按下Enter 生成，Shift+Enter 换行）”
参数控件行（按钮化重构）
整体布局：将原垂直参数列表改为水平紧凑行，位置在预览区域下方，参考图二底部控件栏：
行高度：40px（与截图一致）
控件间距：8px（水平排列，无垂直空白）
每个参数项必须为可点击按钮（样式与您提供的三张截图完全一致）：
模型选择按钮
显示当前模型名（如即梦4.5），左侧带小图标（参考截图中Banana Pro的图标样式），右侧带下拉箭头，点击弹出模型列表；
分辨率按钮
显示当前值（如2K），左侧带分辨率图标（参考截图的1K图标）
点击弹出分辨率菜单
比例按钮
显示当前值（如1：1），左侧带比例图标（参考截图的Auto图标）
点击弹出比例菜单（样式参考截图：1:1/9:16/16:9...列表）
生成按钮
位置更改为提示文字区右下方，圆形图标，蓝色
其他参数
统一按钮样式
关键细节：
所有按钮：12px 字体，浅灰色文字（#666），圆角 4px，背景色 #F5F5F5（浅灰底，适配白色背景）
3. 绝对禁止项（再次强调）
背景色：必须保持langflow原生风格（不可使用图片的深色背景）
功能逻辑：预览框的放大/缩放交互与原节点一致（仅改视觉样式）
上传按钮弹出的页面仅需实现基础上传功能（无需新增搜索/联网等逻辑）
文字内容：仅允许新增 上传图片 按钮文字和顶部提示文字
交互逻辑：参数按钮点击后必须弹出独立下拉菜单（非原地展开）
