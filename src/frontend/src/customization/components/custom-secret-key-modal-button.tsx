export const SecretKeyModalButton = ({
  userId,
  modalProps,
}: {
  userId: string;
  modalProps;
}): JSX.Element => {
  void userId;
  void modalProps;
  return <></>;
};

export default SecretKeyModalButton;
